# HISTORY — TickRevamp

- 2026-09-16 — Workspace created from Jake's request. `reference/` populated from HeroicSouls-BackUp @ fdb6efba8
  and rxi/tick master (hashes in `reference/SOURCES.md`); `build/tests/run.luau` ported from TickAPIOptimize;
  Wave 1 research workflow launched (8 agents).
- 2026-09-16 — Wave 1 complete (8 reports, 8/8). Decisions brief `docs/DECISIONS-BRIEF.md` written; `reference/lune/Tick.luau` legacy oracle copied from TickAPIOptimize.
- 2026-09-16 — Wave 2 design panel complete (run `wf_2b057cbd-3ce`): candidates `lean`/`complete`/`stable` (judges: stable 141.9 > lean 130.6 > complete 130.5); `docs/DESIGN.md` synthesized from "stable" + grafts; refuters found 2 critical (base-carry leak across a yielding callback; stop cascade double-stopping a Complete()d chained child) + 9 major, all applied by the reviser (§13 holds 6 deferred minors). `docs/QUESTIONS.md` written; `build/vendor/BaseClass.luau` created (test-only, hash pinned). Build NOT started — Jake asked to review the design first.
- 2026-09-16 — Jake answered `docs/QUESTIONS.md`: SCOPE NARROWED to the `Tick` class only (no TickAPI registry/hooks/SafeStopClock/AfterNotTouched/keyed timers); `recur(fn, period, uncapped)` boolean; `MaxDt` config default off; recur-nested timing fixed with `LegacyRecurBase` flag + migration warning; `Complete()` deferred; `+inf` rejected. Rescope workflow `wf_dec37679-936` launched on `docs/DESIGN.md`.
- 2026-09-16 — Jake: the build is named **Veron** (class `Veron`, handle `VeronEvent`); add `Toggle()` and `Restart()` for the touch patterns (reset-after-touched, recur-till-touched, recur switch) and document the reuse-over-recreate rule. To be folded into `docs/DESIGN.md` after the rescope run completes.
- 2026-09-16 — Design rescoped to Tick-only (`wf_dec37679-936`, 4 majors fixed) then renamed to **Veron** with `Toggle()`/`Restart()`/reuse rule (`wf_89db0e92-59f`; checker found 1 critical: self-Restart of a zero-period one-shot bypassed the valve → fixed by re-issuing the Id; Stop on Fired now finalizes). Build wave `wf_1901e32d-197` launched, then STOPPED at Stage A on Jake's request.
- 2026-09-16 — optimize-context pass before compaction: `HANDOFF.md` rewritten as a lean cold-start cursor; `docs/QUESTIONS.md` compressed to Jake's verbatim answers (the original question text / alternatives now live in `docs/DESIGN.md` §8 and the vault note `TickRevamp`); `docs/DECISIONS-BRIEF.md` given a status header naming the decisions superseded by the Tick-only scope; the build workflow script copied to `plans/veron-build.workflow.js`; `CLAUDE.md` layout updated (run_one, plans/, Env seam); vault `Projects/TickRevamp/TickRevamp.md` refreshed to the Veron design + Jake's decisions; `Projects.md` and `Roblox Studio Task Workspaces` rows updated. `/mapThis` deliberately skipped: no source exists yet.
- 2026-09-16 — Build (Wave 3) launched on Jake's "resume build" after compaction: run `wf_3c86bda5-453` from `plans/veron-build.workflow.js`; HANDOFF state row flipped to RUNNING.
- 2026-09-16 — Build GREEN (278 tests, 0 fail); `docs/FINDINGS.md` Status column flipped from `planned` for all 134 rows, evidence-backed against `build/tests/spec/{regress/repros,parity,heap,scheduler/*,handle/*,errors,baseclass,env,alloc,docs}_spec.luau` + BUILD-REPORT/DESIGN §8/§13. Counts: 71 fixed (each cited in a new `## Pins` appendix), 27 n/a (scope: TickAPI wrapper / AfterNotTouched / SyncedTimer / RunService / driver), 36 n/a (evidence: measurements, comparisons, censuses, Roblox/BaseClass facts), 0 deferred (the §13 minors are design-stage ids, not ledger rows), 0 still-planned. A `## FLAGS` section records three fixed rows whose shipped behaviour deliberately diverges from the finding's assumption (CS-04 Complete deferred not immediate; B-14 Chained GetRemaining == delay; B-15 group-time kept).
- 2026-09-16 — Build COMPLETE. Run `wf_3c86bda5-453` finished all 13 agents (WP1–WP9; first pass hit a session limit on WP7/WP8/final, resumed clean from cache). Source in `build/src/Veron/{init,VeronEvent/init,Env/init}.luau` (2555 lines); 20 spec files; `build/bench/{bench,studio_bench}.luau`+`BENCH.md`; `build/docs/{API,MIGRATION}.md`; `build/{BUILD-NOTES,BUILD-REPORT}.md`. Suite 278/0; bench 21 invariant checks/0 fail. Veron vs legacy: ~3100× idle, 2.2× recurring, 7.7× churn; 0 B garbage on idle + recurring paths. Known gaps (BUILD-REPORT §6): Lune-only (no Roblox run yet); 4 interpreter timing targets missed (hard zero-garbage asserts pass); 3 design-text corrections for Jake (valve latch wording, 560 vs 576 B, DV-7 line 346).
- 2026-09-16 — Wave 4 adversarial code review: 3 fresh reviewers (correctness/concurrency, memory/heap, API/owner-fit), each building live Lune repros. Result CLEAN — 0 correctness defects, 0 memory/heap defects. One doc-only fix (RV-1): `build/docs/API.md` terminal-totality sentence corrected — `Complete`/`CompleteNow` validate opts BEFORE the terminal check, so a bad opts table on a dead handle still raises (docs_spec still 9/9). One informational by-design note (RV-S1): a paused one-shot parent retains stopped children's memory until it fires (bounded, documented). No code change needed.
- 2026-09-16 — mapThis: build "TickRevamp - Map" (12 systems, 0 unmapped); registered in the project note, `CLAUDE.md` and `HANDOFF.md` point at it.
- 2026-09-16 — Veron VERIFIED in real Roblox Studio. Synced into place `TickAPI_Refactor` via Rojo (`veron-studio.project.json` + `rojo serve`); Play-mode battery `build/studio/VeronRun.server.luau` printed **pass=50 fail=0** under the true Roblox VM — real require branches (`Env.IsRoblox==true`), BaseClass class registry, dot/colon, aliases, capped+uncapped catch-up, re-entrancy, error isolation, chaining, foreign-handle rejection, state machine, heap order. Bench (interpreted Play, 10k): arm 390 ns/op, fire 425 ns/dispatch, 200 idle updates over 10k armed = 0 KB GC. One test fixed mid-run (float-boundary: 0.01 period → dyadic 1/64; not a Veron bug — uncapped Dropped==0 proved nothing lost). Closes the BUILD-REPORT §6.1 'never run in Roblox' gap (native `--!native` + Studio profiler bench still a manual step). New files: `veron-studio.project.json`, `build/studio/VeronRun.server.luau`; rojo pinned in `aftman.toml`; `/roblox-init` installed the `roblox-studio` skill.
- 2026-09-16 — Head-to-head vs legacy rxi Tick in real Roblox (`build/studio/ComparePerf.server.luau`, both synced by Rojo). Veron wins the real-game cases: idle 2400×, handle-stop 18×, mixed churn 5.4×, varied-period recur 4.9×. Legacy wins the two all-fire-at-once cases (fire storm ~4×, all-fire recur ~7×) where the heap's selectivity buys nothing vs a flat array; arm ~tie. Numbers + reading in the vault note.
- 2026-09-16 — Source comments rewritten to LuaCraneStyle across all three Veron modules (`build/src/Veron/{init,VeronEvent/init,Env/init}.luau`): a `--[[ ]]` purpose block on every function (111 blocks total: 85 in init, 24 in VeronEvent, 2 in Env, plus rewritten file headers), plain engineer voice with short examples. Removed every spec/tracker fingerprint (section refs, decision/answer/rule/finding ids, doc citations, build-provenance) and every AI tell (em/en dashes, "Note:/Notably/..." openers, curly quotes, symbol glyphs). CODE FROZEN: verified byte-identical code (init 1538 code lines, 0 diff vs backup; comment-only edits) and suite still 278/0. No design or behavior change, so no vault/spec doc update.
- 2026-09-16 — Jake decided to DROP the BaseClass/middleclass dependency: Veron uses no OOP feature of it (no inheritance/subclassing/mixins) and it forced `--!nonstrict`. Fork `aac88deaa306106b0` converting `Veron` and `VeronEvent` to plain typed modules (hand-rolled metatable + `.new` factory), preserving the observable surface (`class`/`type`/`isInstanceOf`/refused construction/colon guard/aliases); updating the oracle and rewriting the `baseclass_spec`/`env_spec`; kept nonstrict. IN PROGRESS at compaction. Rationale + typing plan in the vault note; preference captured in memory `jake-clean-lean-code`. `build/vendor/BaseClass.luau` becomes unused (file kept).
- 2026-09-16 — optimize-context pass (prep for compact): HANDOFF state table refreshed to current reality (build/review/Studio/comparison/comment-pass done; BaseClass removal in flight; exported-types/IntelliSense pending) with an 'In flight' section; memory `jake-clean-lean-code` added + indexed; vault note status + middleclass decision updated. Map NOT refreshed (deliberate: refactor in flight; run `/mapThis TickRevamp` after it lands, since the Env and vendored-BaseClass rows go stale once BaseClass is gone).
- 2026-09-16 — BaseClass removal COMPLETE + verified. Fork `aac88deaa306106b0` converted `Veron`/`VeronEvent`/`Env` to plain modules (module table is its own metatable; `.new` factory + `__call`; forward-declared `_construct` local keeps error levels; Env down to 5 keys). Observable surface preserved. Lune suite **276/0** (278→276: two vendored-BaseClass file tests removed from env_spec; oracle + baseclass_spec rewritten). Real-Roblox re-verify via Rojo: `VeronRun` **51/51** including new plain-instance checks (handle metatable == VeronEvent, scheduler == Veron, isInstanceOf). `grep BaseClass build/src` empty; `build/vendor/BaseClass.luau` unused, kept. Still nonstrict; exported types are the next step.
- 2026-09-16 — optimize-context (second compact-prep): swept the stale BaseClass claims left after the removal — `CLAUDE.md` header (BaseClass-built → plain-Luau) and vendor row (marked unused), and both vault index rows (`Projects.md`, `Roblox Studio Task Workspaces`) refreshed to plain modules / Lune 276/0 / Roblox 51/51 / exported-types-next. Map paths all still resolve; its BaseClass-wording refresh stays bundled with the exported-types session (no path broke).
- 2026-09-17 — Full battery re-run at Jake's request ("one last big test"). Lune suite **276/0**. The benchmark, the one executable artifact NOT re-run after the BaseClass removal, was BROKEN by it — three stale-API refs surfaced in order: `Env.BaseClass` (header, nil since Env dropped the key), then `VeronEvent.__instanceDict`/`Veron.__instanceDict` (body lines 81-82, the pre-refactor dicts). Fixed in both `bench.luau` (Lune header now requires `../vendor/BaseClass` as a comparator, dead `Env` require dropped) and `studio_bench.luau` (header requires `SharedModules.BaseClass`); body lines repointed to `VeronEvent`/`Veron` in BOTH files, bodies re-verified byte-identical (796 lines). Bench now **21/0** invariant checks: 0-garbage idle/recur, 560 B/handle, plain-handle vs Class:new ratio 0.18 (<0.3), Veron 2.2×-7.5× over legacy. Timing targets MISSED as always under interpreted Lune (non-asserting; native Studio is the real timing). Also swept stale API names out of the docs: `VeronEvent.static.STATE_NAMES`→`VeronEvent.STATE_NAMES` (`API.md`), `Veron.static._Heap`→`Veron._Heap` and `VeronEvent.__instanceDict`→`VeronEvent` (`BUILD-REPORT.md`, `BUILD-NOTES.md`). `build/src` and all docs now free of dead BaseClass-era API; vendor BaseClass.luau's own `__instanceDict` left (it IS BaseClass).
- 2026-09-17 — Real-Roblox full battery + head-to-head, driven by me via the Studio MCP (Jake: I hold the tools, do not defer Play to him — memory `studio-mcp-i-drive`). Place `TickAPI_Refactor` still held the Rojo-synced tree; verified the live source is plain-module (Veron 2456 / VeronEvent 379 / Env 71 lines, 0 BaseClass/`__instanceDict`/`.static.`), then Play → `VeronRun` **51/51 features** + `ComparePerf` fresh: idle **2351×**, handle-stop **15.6×**, mixed churn **4.2×**, varied recur **4.8×** (Veron); legacy wins only fire-storm (0.3×) and all-fire recur (0.1×). Lune cross-check (temp twin of ComparePerf, run + deleted) agreed: idle 3855×, cancel 4.6×, churn 3.1×, varied 4.5×. Built the breakdown chart artifact (log-scale diverging bars): https://claude.ai/code/artifact/9d6b05b6-22fe-411d-9e95-fc0da2c0090b . Stopped Play (place left unsaved — human only).
- 2026-09-17 — BaseClass doc sweep (Jake asked to confirm it is fully gone) + install docs. `API.md`: dropped the two stale claims that Veron/VeronEvent are "built with BaseClass" (now: plain typed modules; construction reaches one `_construct` frame, no vendor `__call`); added a top "Wire it up: register and drive" quickstart (register `Veron.new`, connect ONE RunService.Heartbeat to `Update(dt)`, arm timers) — the hookup Jake could not find from the API alone. `BUILD-NOTES.md` lines 106/234 marked superseded by the removal. Remaining BaseClass mentions in docs are the vendor snapshot + the bench P11 comparator only. Suite still 276/0 (docs_spec green; 576 B wording left as-is — it is pinned by docs_spec and is a separate Jake sign-off).
- 2026-09-17 — **Release-readiness sweep** (before this folder becomes a GitHub repo; via Herdr/Hermes). Removed the last BaseClass traces and vestigial code from executable/build code. (1) Bench **P11** (the handle-literal vs BaseClass `Class:new` construction comparator) cut from `bench.luau` and `studio_bench.luau` — the `BaseClass` require/dep, the `BenchEvent12` root class, the `LITERAL_RATIO` target, and the P11 phase; the two bodies re-verified byte-identical; bench now **20/0** (was 21/0 — P11 held the one dropped check). (2) `build/vendor/BaseClass.luau` **deleted** (its only remaining consumer was bench P11) and the empty `build/vendor/` folder removed. (3) `veron-studio.project.json`: the `ReplicatedStorage.SharedModules.BaseClass` mapping dropped (no synced script required it — VeronRun/ComparePerf use `ReplicatedStorage.Veron`+`LegacyTick`). (4) Misleading test path **renamed**: `build/tests/spec/baseclass/baseclass_spec.luau` → `build/tests/spec/class/class_spec.luau` (same depth, relative requires unchanged; runner auto-discovers; 11 tests intact). (5) Stale VeronRun header comment ("real BaseClass class registry") corrected. (6) Current-facing docs corrected: `CLAUDE.md` (vendor row removed, Rojo/Studio note), `BENCH.md` + `BUILD-REPORT.md` + `BUILD-NOTES.md` (P11 row/refs, vendor manifest line, spec-name pointers; BUILD-REPORT §6 gap #1 rewritten — Studio HAS run 51/51, only `<native>` + a post-sweep `studio_bench` re-run remain), `DESIGN.md` (a dated supersession banner mapping every stale BaseClass term to the plain-module reality, plus surgical §1/§10/§11 fixes and the `baseclass_spec`→`class_spec` pointer rename), `FINDINGS.md` and `.claude/skills/roblox-studio/SKILL.md` (spec-name + deleted-vendor pointers). Verified: **Lune 276/0, bench 20/0**; `grep -ri baseclass` over executable/build code (outside `reference/`, `research/`) empty of live code. Kept as clearly-labeled history: `reference/` (read-only), `research/` incl. `baseclass-conventions.md` + scratch, `docs/design-candidates/*`, `docs/QUESTIONS.md` + `DECISIONS-BRIEF.md` (Q-J15 verbatim BaseClass decision — predates the drop), `plans/veron-build.workflow.js`. Kept deliberately: bench **P14** + `ComparePerf` legacy-`Tick` comparators (the thing Veron replaces, a core deliverable — not BaseClass). Studio-side re-run of `studio_bench`/VeronRun/the trimmed Rojo tree is the one residual verification (reported to Hermes; not driven in this sweep).
