export const meta = {
  name: 'veron-build',
  description: 'Wave 3: build Veron from docs/DESIGN.md — Env/support/VeronEvent in parallel, then the Veron core with parallel spec writers, integrate to green, then parity/repros, alloc/bench and docs, final integration',
  phases: [
    { title: 'Foundations', detail: 'WP1 Env, WP2 test support, WP3 VeronEvent' },
    { title: 'Core', detail: 'WP4+5+6 Veron core (one author) + 4 spec writers' },
    { title: 'Integrate', detail: 'run suite, triage, fix to green' },
    { title: 'Proof', detail: 'WP7 parity+repros, WP8 alloc+bench, WP9 docs' },
    { title: 'Final', detail: 'whole suite green + bench + build report' },
  ],
}

const CAP = 10
let live = 0
const waiters = []
const slot = () => new Promise(res => { if (live < CAP) { live++; res() } else waiters.push(res) })
const release = () => { const w = waiters.shift(); if (w) w(); else live-- }
async function A(prompt, opts) { await slot(); try { return await agent(prompt, opts) } finally { release() } }

const W = 'C:/Users/Faded/Documents/ClaudeProjects/RbxProjects/STUDIO_TASKS/TickRevamp'

const SHARED = `
## Shared context (read fully before writing anything)

You are one builder in the **Veron** build (workspace W = ${W}). Veron is a Roblox/Luau timer scheduler class (one instance per RunService hook; indexed min-heap; due-time dispatch) built with the house BaseClass. Your output is DATA for an orchestrator, not prose for a human.

THE DESIGN IS THE AUTHORITY: ${W}/docs/DESIGN.md (979 lines). Read the sections your package needs IN FULL (the header rules R-A..R-D, §1, §2, §3 always; §4 for code; §5 state table; §9 for the exact test names; §12 for your acceptance list). Implement exactly what it says; where it is silent, pick the simplest option consistent with it and record the choice in ${W}/build/BUILD-NOTES.md (append a dated bullet under a heading with your package id; create the file if missing). Never contradict a settled decision.

Other authorities: ${W}/CLAUDE.md (Lune require rules, hard rules); house style ${'C:/Users/Faded/Documents/GitHub/LuaCraneStyle/wiki'}/{Global-Rules,General-Lua,BaseClass-MiddleClass,Pure-Lua-Modules}.md — tabs, double quotes, PascalCase public members, camelCase locals/params, \`_camelCase\` file-local helpers, \`_PascalCase\` private instance fields, \`--[[ ]]\` purpose blocks on non-trivial functions, 80-col target / 120 hard limit, guard clauses, no single-letter names EXCEPT inside the marked \`--[[ HOT PATH … ]]\` blocks (heap ops, \`_pass\`) which transcribe the verified prototype in ${W}/research/scratch/algos/invariants.luau / bench.luau (functions ib_siftUp/ib_siftDown/push/pop/remove/updateKey) as-is.

Toolchain: Lune 0.10.5 (\`lune\` on PATH; fallback C:/Users/Faded/.aftman/tool-storage/lune-org/lune/0.10.5/lune.exe). Run from W: whole suite \`lune run build/tests/run.luau\`; ONE spec \`lune run build/tests/run_one.luau spec/<area>/<name>_spec\`. Spec shape: \`return function(t) t.test("name", function() … end) end\` with \`t.eq(actual, expected, msg?)\`, \`t.near(a, b, tol?, msg?)\`, \`t.throws(fn, pattern?)\`; file names end in \`_spec.luau\` with no other dot. Test names MUST be exactly the §9 names (acceptance is \`spec path :: test name\`).

Lune require rules (verified): string paths, no extension; \`X/init.luau\` is required as \`"…/X"\`; in a plain file \`./\` is the file's own directory; INSIDE an init.luau \`./\` is the directory that CONTAINS the module directory; \`game\` and \`script\` are nil under Lune; absolute paths are NOT accepted. Concretely: from \`build/tests/spec/<area>/<name>_spec.luau\`: the class is \`require("../../../src/Veron")\`, the handle class \`require("../../../src/Veron/VeronEvent")\`, Env \`require("../../../src/Veron/Env")\`, support \`require("../../support/oracle")\` / \`"../../support/legacy"\` / \`"../../support/gen"\`, the legacy oracle \`require("../../../../reference/lune/Tick")\`, the vendor BaseClass \`require("../../../vendor/BaseClass")\`. From \`build/src/Veron/init.luau\`: \`require("./Veron/Env")\`, \`require("./Veron/VeronEvent")\` (Roblox: \`script.Env\`, \`script.VeronEvent\`). From \`build/src/Veron/VeronEvent/init.luau\`: \`require("./Env")\` (Roblox: \`script.Parent.Env\`). From \`build/src/Veron/Env/init.luau\`: \`require("../../vendor/BaseClass")\` (Roblox: \`require(game:GetService("ReplicatedStorage"):WaitForChild("SharedModules").BaseClass)\`). Branch with \`if typeof(script) == "Instance" then … else … end\`; files that read \`script\` are \`--!nonstrict\`.

BaseClass facts (vendor ${W}/build/vendor/BaseClass.luau = live file, one patched line): \`BaseClass.class("Name")\` → class; \`Class:new(...)\` → allocate + \`:initialize(...)\` + \`instance.type = tostring(instance)\`; \`Class.__instanceDict\` is the instance metatable (\`__index\` = itself); \`Class.static\` holds statics; methods are declared \`function Class:Method()\` AFTER creation; class names are registered process-wide (a duplicate WARNS) so require each class module once; \`instance.class\` and \`instance.type\` fields exist on real instances — a literal \`setmetatable({class = Class, type = "Name", …}, Class.__instanceDict)\` is an equivalent instance.

HARD RULES: write ONLY the files your package lists (plus BUILD-NOTES.md bullets). Never modify W/reference, W/research, W/docs, the sibling TickAPIOptimize workspace, or HeroicSouls-BackUp. No git. Callbacks are untrusted. Every file you write must at least LOAD under Lune (\`lune run\` a throwaway requiring it, under build/tests/scratch/<pkg>/, delete it after). Return the structured output when done.
`

const OUT = {
  type: 'object',
  properties: {
    files: { type: 'array', items: { type: 'string' } },
    summary: { type: 'string', description: '<= 1200 chars' },
    testsRun: { type: 'string', description: 'the exact command(s) you ran and their last line, e.g. "lune run build/tests/run_one.luau spec/env/env_spec → 5 passed, 0 failed"' },
    deviations: { type: 'array', items: { type: 'string' }, description: 'every place you departed from DESIGN.md or filled a gap, with the section' },
    blockers: { type: 'array', items: { type: 'string' } },
  },
  required: ['files', 'summary', 'testsRun', 'deviations', 'blockers'],
}

// ------------------------------------------------------------------ Stage A
phase('Foundations')
log('Stage A: WP1 Env, WP2 test support, WP3 VeronEvent (parallel)')
const stageA = (await parallel([
  () => A(SHARED + `
## Your package: WP1 — Env seam (DESIGN §1 Env row, §12 WP1) + its spec
Write ${W}/build/src/Veron/Env/init.luau: a plain table with EXACTLY \`IsRoblox\`, \`BaseClass\`, \`Warn\`, \`Traceback\`, \`Load\`, \`SetWarn\` (returns a restore function; scoped). Under Lune load the vendor (\`require("../../vendor/BaseClass")\`); on Roblox \`require(game:GetService("ReplicatedStorage"):WaitForChild("SharedModules").BaseClass)\`. \`Load(name)\` = \`require(script.Parent[name])\` on Roblox / \`require("./" .. name)\` under Lune (relative to Env/init.luau, i.e. a sibling module directory such as "VeronEvent" — verify with a throwaway that \`Env.Load("VeronEvent")\` resolves once WP3 exists; if WP3 is not there yet, test Load against a throwaway sibling you create and delete under build/src/Veron/_probe/init.luau).
Write ${W}/build/tests/spec/env/env_spec.luau with exactly the five §9 env tests: \`Lune load resolves the vendored BaseClass\`; \`vendor BaseClass differs from reference only at the JsonR lines\` (read both files with @lune/fs — reference at "../../../../reference/baseclass/BaseClass.luau" is a path for fs, not require: compute it from the spec file's location or use process.cwd() = W; assert every differing line is within the patched RJson block, and pin sha256 prefix 21960b8e125d1cd5 of the vendor via @lune/serde hash if available, else skip the hash with a note); \`SetWarn restores\`; \`Load resolves siblings\`; \`IsRoblox is false under Lune and Env exposes exactly IsRoblox/BaseClass/Warn/Traceback/Load/SetWarn\`. Run it green.`, { label: 'WP1:Env', phase: 'Foundations', schema: OUT }),

  () => A(SHARED + `
## Your package: WP2 — test support (DESIGN §9 first paragraph, §3.2 invariants, §12 WP2)
Write three files under ${W}/build/tests/support/ (no _spec suffix):
1. \`oracle.luau\` — \`Oracle.check(veron)\`: asserts \`veron:Validate()\` is true AND independently re-derives every §3.2 invariant from the instance's private fields (\`_Due/_Item/_N\`, heap property on (due, Id), \`_Item[h._HeapIndex] == h\` and \`_State == 1\` for every slot, \`#_Due == #_Item == _N\`, \`_Paused\` ⇔ state 2 with \`_HeapIndex == 0\`, \`_PausedCount\`, no NaN/inf dues, \`_ChainedCount\`, \`_RecurCount\`, \`_ChildCount\`, terminal handles have \`_HeapIndex == 0\` and \`_Flags == 0\`, Stopped have \`_Fn\` == the NOOP (compare against a Stopped handle's \`_Fn\` captured once), \`_Flags >= 4\` only on Pending recurring, and \`_UpdateDepth == 0 and _SyncDepth == 0 ⇒ _Base == false and _Firing == false and _FiringThread == false\`) so a broken Validate cannot hide a broken heap; raises with a precise message naming the first violated invariant. \`Oracle.wrap(veron)\` — returns the SAME instance after replacing each of its public per-instance closures (every key of the instance whose value is a function: the design installs dual dot/colon closures on the instance in initialize) with a version that calls the original then \`Oracle.check(veron)\`, and, ONCE per process, wraps every public method of \`VeronEvent.__instanceDict\` (require the class through "../../src/Veron/VeronEvent") with a version that calls the original then checks \`self._Scheduler\` (skip the guard-only failure path: if the original raises, re-raise unchanged with \`error(err, 0)\`). Re-check inside callbacks is achieved naturally because callbacks call wrapped methods. Provide \`Oracle.unwrap()\` no-op for symmetry. Wrapping must not break the colon guard (methods still receive the real handle) nor the dual dot/colon detection (the wrapper must accept both forms: forward all args unchanged).
2. \`legacy.luau\` — adapters over the legacy Lune shim: \`Legacy.group()\` returns \`require("../../reference/lune/Tick").group()\` (check the path: from build/tests/support/, "../../reference" is build/reference — WRONG; the workspace reference is at W/reference, so use "../../../reference/lune/Tick"); plus helpers \`Legacy.delay(g, fn, t)\`, \`Legacy.recur(g, fn, t)\`, \`Legacy.update(g, dt)\`, \`Legacy.stop(h)\`, \`Legacy.reset(h)\`, \`Legacy.adjust(h, x)\`, \`Legacy.after(h, fn, t)\` that call the shim's colon methods, so a parity harness can drive legacy and Veron with one step list.
3. \`gen.luau\` — a seeded deterministic op generator (\`Gen.new(seed)\` with a simple LCG or xorshift, NOT math.random): \`gen:sequence(n, constraints)\` returns a step list of {op = "delay"|"recur"|"stop"|"reset"|"adjust"|"after"|"nest"|"update", …} using DYADIC delays (k/64 with k in 1..64) and dt = 1/64 or 1/32, honouring the §9 parity constraints (no stop/reset/adjust from inside callbacks, no nest inside recur callbacks, no nested delay ≤ overshoot — encode "nest" as a delay created inside a ONE-SHOT callback with delay ≥ 1/16), with an option \`lagLimit\` so a "default cap" generator never lets a recurring lag more than 8 periods.
Write a smoke spec ${W}/build/tests/spec/support/support_spec.luau (2–4 tests: legacy adapter fires a delay; generator is deterministic for a seed and honours constraints; oracle.check raises on a hand-built inconsistent fake instance) and run it green. The real Veron class may not exist yet when you run: design Oracle so requiring VeronEvent is lazy (inside wrap) and check() works on any table with the listed fields.`, { label: 'WP2:support', phase: 'Foundations', schema: OUT }),

  () => A(SHARED + `
## Your package: WP3 — VeronEvent handle class (DESIGN §1, §2.3, §3.1, §4.10, §5, §12 WP3) + its baseclass spec
Write ${W}/build/src/Veron/VeronEvent/init.luau: outer module table \`VeronEventClass\` (\`_VERSION = "3.0.0"\`, \`_DESCRIPTION\`, a \`--[[ NOTES ]]\` block), the Env seam (\`require("./Env")\` under Lune / \`script.Parent.Env\` on Roblox), \`local VeronEvent = Env.BaseClass.class("VeronEvent")\`, \`VeronEvent:initialize()\` that ONLY raises \`VeronEvent: handles are created by a Veron (Delay/Recur/After)\` at level 3, numeric state constants (Pending 1 … Stopped 5) and \`VeronEvent.static.STATE_NAMES = {"Pending","Paused","Chained","Fired","Stopped"}\` (frozen), the colon guard on EVERY method (\`if getmetatable(self) ~= eventDict then error("VeronEvent: methods take a colon: event:Stop(), not event.Stop()", 2) end\` where \`eventDict = VeronEvent.__instanceDict\`), and thin delegating methods per §2.3/§4.10: Stop (→ \`self._Scheduler:_Stop(self)\`, returns nothing), Reset, Adjust, After, SetPeriod, GetPeriod, SetRemaining, GetRemaining, Pause, Resume, IsPaused, Toggle, Restart, Complete(opts), CompleteNow(opts), SetCatchUp(mode, n), GetState, GetStateId, IsActive, IsRecurring, GetId, GetScheduler, Describe — each returning what §2.3 says (\`h\` for the fluent ones). Queries read own fields only; Describe reads \`self._Scheduler.Name\`. IsPaused = state Paused OR (Pending with \`_Flags >= 8\`). Then the LEGACY SURFACE region: \`VeronEvent.stop = VeronEvent.Stop\`, \`reset\`, \`adjust\`, \`after\`, \`Destroy = Stop\` (same function objects). No \`__index\`, no \`__tostring\`. Do NOT construct handles anywhere; the scheduler builds the 14-key literal (§3.1) — but export nothing else. Keep the design's error levels (handle methods raise at level 2; scheduler _Op methods at 3).
Write ${W}/build/tests/spec/baseclass/baseclass_spec.luau with the §9 baseclass tests that do NOT need the Veron core (\`handle isInstanceOf VeronEvent, class and type set on a literal handle\` — build a §3.1 literal by hand in the test with a fake \`_Scheduler = { Name = "T" }\`; \`handle literal has exactly 14 keys, every value non-nil\`; \`Fn/Period/IsRecur are not public fields\`; \`VeronEvent:new and VeronEvent() raise the construction error\`; \`no __index or __tostring declared on any class\` (VeronEvent only for now; leave a TODO comment for Veron); \`requiring every class module prints no "already Exsist" warning\` (VeronEvent for now)) and the handle test \`handle dot-call raises the colon message\` in ${W}/build/tests/spec/handle/state_spec.luau (create the file with just this one test; the WP4 spec writers will ADD the rest — leave a clear comment). Run both green. Note in BUILD-NOTES.md which baseclass tests are deferred to WP4/WP6.`, { label: 'WP3:VeronEvent', phase: 'Foundations', schema: OUT }),
])).filter(Boolean)
log(`Stage A done: ${stageA.length}/3; blockers: ${stageA.flatMap(r => r.blockers).length}`)

// ------------------------------------------------------------------ Stage B
phase('Core')
log('Stage B: Veron core author + 4 spec writers (parallel)')
const SPEC_COMMON = `
## Spec-writer rules
You write TESTS ONLY, from the design's API contract, while another agent writes the Veron core in parallel — the class may not exist or may be incomplete when you run. Write every test named in §9 for your files with the EXACT names, structured so it will pass against a correct implementation of DESIGN.md. Use \`Oracle.wrap(Veron.new{ Name = "T" })\` (support/oracle.luau) unless the design marks the spec raw; capture warnings with \`Env.SetWarn\`; use dyadic numbers (1/64, 1/1024) for timing so float boundaries are exact; for yield/coroutine tests use \`coroutine.create/resume/yield\` around \`Update\` (a callback that yields suspends the pass; resume it later). For "valve" tests use \`Veron.new{ Valve = 100 }\` where the design's numbers say so. Where a §9 test needs an internal field (e.g. \`_Fn == NOOP\`), read it directly (\`rawget(h, "_Fn")\`) and compare with another Stopped handle's \`_Fn\`. If the core exists when you finish, run your spec with run_one and report the result; failures against an incomplete core are EXPECTED and not your problem — but a test that contradicts DESIGN.md is. List per test: the §9 name, and the DESIGN section it derives from. Write nothing outside your spec files (+ BUILD-NOTES.md).`

const stageB = (await parallel([
  () => A(SHARED + `
## Your package: WP4 + WP5 + WP6 — the Veron core, ONE file: ${W}/build/src/Veron/init.luau
You are the single author of the scheduler. Read DESIGN.md §1–§5, §4 in full (every code block and comment), §9 (the behaviours tests will assert) and §12 WP4/WP5/WP6. Also read ${W}/research/scratch/algos/invariants.luau (the verified indexed-heap prototype: transcribe its sift/push/pop/remove/updateKey as the HOT PATH block with the renames in §3.2) and ${W}/build/src/Veron/VeronEvent/init.luau (WP3, already written — its method → \`_Op\` contract is §4.10; if it is missing something the design requires, note it in BUILD-NOTES.md and the integrator will fix it).
Write the whole class in this order (the §4 module head order): \`--!native --!nonstrict\`; outer table \`VeronClass\` (\`_VERSION = "3.0.0"\`, \`_DESCRIPTION\`, \`--[[ NOTES ]]\`); Env seam + VeronEvent require; constants (state ids, flag bits, HUGE, NOOP, localized globals, \`running = coroutine.running\`, \`traceback = Env.Traceback\`, \`eventDict = VeronEvent.__instanceDict\`); forward declarations \`local _advanceTo, _stopTerminal, _attach\`; the HOT PATH heap ops (siftUp, siftDown, heapPush, heapPop, heapRemove, heapUpdate, heapClear over \`self._Due/_Item/_N\`, handle \`_HeapIndex\`, (due, Id) ordering); \`_isCallable/_clockOf/_checkOpts/_validateUncapped/_validateArgs/_newEvent\`; \`_stopTerminal\` (§4.3: state-gated cascade; finalizes a Fired handle: → Stopped, \`_Fn = NOOP\`, returns false); \`_resolve\`; \`_diagnose/_report/_runCallback/_fireOneShot/_fireRepause/_valveHit/_stopResolution\`; \`_pass\` (§4.2 verbatim semantics: valve on \`h.Id > startId\`, capped snap-to-grid, uncapped repeat counting, sub-resolution guard, LegacyRecurBase, re-key BEFORE the callback, nothing after); \`_advanceTo\` (backwards clamp, depth counter, idle fast path, protected pass under xpcall with the unconditional tail, triple save/restore, "error" policy re-raise at the outermost exit, internal-fault re-raise); \`local Veron = Env.BaseClass.class("Veron")\`; \`local veronDict = Veron.__instanceDict\`; \`Veron:initialize(cfg)\` (config validation per §2.1 with the bare \`Veron: \` prefix and level 4; all §3.2 fields assigned non-nil; the dual closures per the §3.2 PUBLIC list, one closure object per canonical member under every alias); the static \`new\` override (§1); public methods (§2.2) and private \`_Op\` methods (§4.4–4.7, §4.9): Update (type check FIRST, destroyed, MaxDt clamp, \`_LastDt\`, paused → return, \`_advanceTo\`), UpdateTo (§4.8), Now/SetTimeScale/GetTimeScale, Pause/Resume/IsPaused, Delay/Recur(fn, p, uncapped)/DelayAt, Stop/Complete/CompleteNow/GetRemaining (through \`_resolve\`), GetClocks, GetStats(into), Validate (§4.11: every §3.2 invariant, returns true | false, reason), Describe, SetOnError/SetMaxCatchUp/GetMaxCatchUp/SetValve/SetMaxDt/SetStrict, Clear (\`_clearTerminal\` walk, §4.3), Attach/Detach/GetChildren/Drive(child, period, uncapped default true, always attaches)/Destroy/IsDestroyed/IsUpdating/GetBase; \`_Stop/_Reset/_Adjust/_SetPeriod/_SetRemaining/_GetRemaining/_Pause/_Resume/_Toggle/_Restart/_After/_Complete/_CompleteNow/_SetCatchUp\` (terminal + COMMITTED checks BEFORE argument validation; \`_Restart\` re-issues the Id on the Fired path); return \`Veron\`. Also expose, for tests only, \`Veron.static._Heap = { push = heapPush, pop = heapPop, remove = heapRemove, update = heapUpdate, clear = heapClear }\` (static table, never on the hot path; note it in BUILD-NOTES.md) so heap_spec can drive the raw heap on a bare instance's arrays.
Correctness beats speed everywhere outside the HOT PATH blocks; inside them keep the prototype shape. Every public instance method must work as \`s.Delay(fn, t)\` and \`s:Delay(fn, t)\`. Error levels per §2. Write the file, then smoke it under Lune with a throwaway (create, delay, recur, update, stop, adjust, pause/resume, complete, after, drive, destroy; call Validate after each) and fix everything you find. Then run \`lune run build/tests/run.luau\` once (whatever specs exist by then), read the failures, fix the ones that are YOUR bugs (a spec that contradicts DESIGN.md is not yours — list it in deviations). Report line count, the smoke result and the suite line.`, { label: 'WP4-6:Veron-core', phase: 'Core', schema: OUT }),

  () => A(SHARED + SPEC_COMMON + `
## Your spec files: \`build/tests/spec/scheduler/arm_spec.luau\` (12 tests), \`scheduler/time_spec.luau\` (16), \`scheduler/dispatch_spec.luau\` (16), \`scheduler/catchup_spec.luau\` (15) — exactly the §9 names.
Derive from §2.1/§2.2, §4.1, §4.2, §4.8 and the legacy validation strings (reference/live/Tick.luau:168-174 — copy the exact templates). For the catchup 'nothing lost' test use the §9 numbers verbatim (1/1024 s uncapped recur, Update(2) → 2048 owed, Valve = 100 → 101 per pass, 21 passes, Dropped == 0, ValveHits == 20). For the valve tests capture the warn-once via Env.SetWarn or a function OnError.`, { label: 'spec:arm-time-dispatch-catchup', phase: 'Core', schema: OUT }),

  () => A(SHARED + SPEC_COMMON + `
## Your spec files: \`build/tests/spec/scheduler/reentrancy_spec.luau\` (20 tests), \`scheduler/lifecycle_spec.luau\` (18) — exactly the §9 names.
Derive from R-C/R-D, §4.2 properties (a)–(j), §4.3, §4.9, §2.4 (the three touch-pattern scenarios in one lifecycle test). Yield tests: run \`s:Update(dt)\` inside \`coroutine.wrap\`/\`coroutine.resume\` with a callback that \`coroutine.yield()\`s; then call \`s:Update\` again from the main thread (overlap), assert siblings fired, then resume the suspended coroutine and assert it drains without double-dispatch and that depth returns to 0 with \`_Base == false\`. The "top-level Delay during a yield window measures from now" test arms from the MAIN thread while the callback is suspended and asserts due == now + t (read \`s:GetBase()\` == false there). The "corrupted handle field" test sets \`rawset(h, "_Period", nil)\` on a Pending recurring and expects the prefixed internal-fault error once with the instance still valid after.`, { label: 'spec:reentrancy-lifecycle', phase: 'Core', schema: OUT }),

  () => A(SHARED + SPEC_COMMON + `
## Your spec files: ADD to \`build/tests/spec/handle/state_spec.luau\` (WP3 created it with one test; keep that test, add the other 19 §9 names), \`handle/stop_spec.luau\` (9), \`handle/retime_spec.luau\` (13) — exactly the §9 names.
Derive from §2.3, §4.3, §4.4, §4.5, §5. "every public method is a no-op on Stopped and, Stop/Restart excepted, on Fired; each returns h" must iterate the real method list of VeronEvent (§2.3 fluent methods) on a Fired one-shot, a Fired recurring (via \`Complete{ Stop = true }\` + update) and a Stopped handle, asserting no state change (compare GetState/GetRemaining/IsPaused before and after) and the return value; the "even with invalid arguments" variant passes the §9 bad arguments and asserts no raise. The colon-form test: \`local TickAPI = { Tick = s }; TickAPI.Tick:remove(h)\` removes.`, { label: 'spec:state-stop-retime', phase: 'Core', schema: OUT }),

  () => A(SHARED + SPEC_COMMON + `
## Your spec files: \`build/tests/spec/handle/chain_spec.luau\` (15), \`handle/complete_spec.luau\` (23), \`heap/heap_spec.luau\` (7, raw: drive \`Veron.static._Heap\` push/pop/remove/update/clear on a bare \`Veron.new{}\` instance's \`_Due/_Item/_N\` with hand-built handle-like tables \`{ Id = n, _HeapIndex = 0, _State = 1 }\`; the ownership test expects the \`Veron: handle is not scheduled on this Veron\` error from update/remove of a foreign table), \`errors/errors_spec.luau\` (4) — exactly the §9 names; and EXTEND \`baseclass/baseclass_spec.luau\` (WP3 wrote the VeronEvent-only tests; add the Veron-dependent ones: \`every Veron field non-nil after initialize\`, \`every public Veron member has a dot and a colon closure\`, \`aliases share one closure object\`, \`all three constructor spellings build the same shape\`, \`legacy alias table is complete\` (exactly: instance update/delay/recur/remove/getClocks + ForceEventComplete/Remove; handle stop/reset/adjust/after/Destroy), and extend the existing no-__index/__tostring and no-duplicate-warning tests to cover Veron).
Derive from §4.6, §4.7, §2 error contract (errors_spec: call each raising entry point from a spec-local function and assert the message's chunk/line prefix names the SPEC file, not Veron/init.luau — pcall the call, then check \`err:match("^(.-):(%d+):")\` names this spec's path; Veron(cfg) is the documented exception), §3.2 heap invariants.`, { label: 'spec:chain-complete-heap-errors-baseclass', phase: 'Core', schema: OUT }),
])).filter(Boolean)
log(`Stage B done: ${stageB.length}/5; core deviations: ${(stageB[0] && stageB[0].deviations.length) || 'n/a'}`)

// ------------------------------------------------------------------ Stage C
phase('Integrate')
const INTEGRATE = (round) => SHARED + `
## Your task: INTEGRATOR round ${round} — make the whole suite green
Run \`lune run build/tests/run.luau\` from W. For every failure decide: (a) the code violates DESIGN.md → fix ${W}/build/src/Veron/init.luau or VeronEvent/init.luau or Env/init.luau; (b) the test contradicts DESIGN.md (wrong expectation, wrong number, misread section) → fix the spec, keeping its §9 name; (c) the design is ambiguous → pick the simplest reading, fix whichever side, and append the decision to ${W}/build/BUILD-NOTES.md under "Integration round ${round}". Never weaken a test to make it pass unless (b) is proven by citing the DESIGN section. Never delete a §9 test. Prefer fixing the code. Repeat run → fix until \`0 failed\` or you have done 6 fix cycles; run a single spec with run_one while iterating. Also read BUILD-NOTES.md and the previous builders' deviation lists (below) and resolve them. Report the final suite line, the number of code vs test fixes, and anything still red with the reason.
Builders' deviations/blockers: ${JSON.stringify([...stageA, ...stageB].map(r => ({ summary: r.summary.slice(0, 300), deviations: r.deviations, blockers: r.blockers })))}`

let integ = await A(INTEGRATE(1), { label: 'integrate:1', phase: 'Integrate', schema: OUT })
let green = integ && /\b0 failed\b/.test(integ.testsRun)
log(`integration 1: ${integ ? integ.testsRun.slice(-60) : 'FAILED'}`)
for (let round = 2; round <= 3 && !green; round++) {
  integ = await A(INTEGRATE(round) + `\nPrevious integrator report: ${JSON.stringify(integ)}`, { label: `integrate:${round}`, phase: 'Integrate', schema: OUT })
  green = integ && /\b0 failed\b/.test(integ.testsRun)
  log(`integration ${round}: ${integ ? integ.testsRun.slice(-60) : 'FAILED'}`)
}

// ------------------------------------------------------------------ Stage D
phase('Proof')
log(`Stage D: parity+repros, alloc+bench, docs (parallel); core green=${green}`)
const stageD = (await parallel([
  () => A(SHARED + `
## Your package: WP7 — parity + repros (DESIGN §9 parity/repros rows, §7.3 DV table, §12 WP7)
Write ${W}/build/tests/spec/parity/parity_spec.luau: a differential harness driving the legacy shim (support/legacy.luau → reference/lune/Tick.luau, \`Tick.group()\`) and \`Veron.new{}\` with ONE step list from support/gen.luau; the generator's recur step arms \`Recur(fn, p, true)\` (legacy is uncapped) for the main runs and capped for the "default cap generator" run; per update compare the SORTED fire lists (label each callback by an id); dyadic numbers. Tests: \`500 seeded op sequences match\`; \`9 scripted prior scenarios match\` (write nine explicit scenario step lists covering delay/recur/after/stop/reset/adjust/nest; the prior build's parity_spec at C:/Users/Faded/Documents/ClaudeProjects/RbxProjects/STUDIO_TASKS/TickAPIOptimize/build/tests/spec/parity/parity_spec.luau is a READ-ONLY model of the shape); \`default cap generator (never lags > 8 periods) matches\`; \`reference file is the live file minus the RunService line\` (fs diff of reference/lune/Tick.luau vs reference/live/Tick.luau); and the 20 two-sided pins \`DV-n …\` — one test per DV row of §7.3 asserting BOTH the legacy value and the new value (e.g. DV-7 legacy 2.500 / new 1.517 at 1/60 and LegacyRecurBase == legacy; DV-24 CurrentType strings; DV-14 MaxDt).
Write ${W}/build/tests/spec/regress/repros_spec.luau: one test per id in the §9 list (33 ids) reproducing the research scenario against Veron and asserting the FIXED behaviour; the scenarios live in ${W}/research/scratch/{legacy,prior,semantics-spec,syncedtimer}/ and the reports ${W}/research/*.md — read the specific id's repro and port it. Run both green (fix only your specs; if a failure exposes a real core bug, do NOT edit the core — report it in blockers with the exact test and scenario).`, { label: 'WP7:parity-repros', phase: 'Proof', schema: OUT }),

  () => A(SHARED + `
## Your package: WP8 — alloc spec + bench (DESIGN §9 alloc row, §10, §12 WP8)
Write ${W}/build/tests/spec/alloc/alloc_spec.luau with the four §9 tests using \`collectgarbage("count")\` deltas (Lune: "count" works; "collect" does not — measure over many iterations and accept 0 KB exactly for the idle/busy/warmed cases as the design demands; if Lune's allocator makes an exact 0 impossible for the busy case, record the measured number in BUILD-NOTES.md and assert ≤ that number with the reason — but first make sure the core really allocates nothing: the design requires it).
Write ${W}/build/bench/bench.luau (phases P1–P12, P14, P15 per §10; seed 20260916; median of 5; prints one line per phase with ns/op, µs/update, bytes, and the invariant assertions; P14 drives reference/lune/Tick.luau as the legacy baseline) and ${W}/build/bench/studio_bench.luau (the same phases as a self-contained Studio command-bar script that requires \`game.ReplicatedStorage.SharedModules.TickAPI.Veron\` and prints the same lines; no Lune requires). Write ${W}/build/bench/BENCH.md with the method, the numbers you measured under Lune, the op-count baseline from §10 P2, and the legacy comparison. Run the alloc spec green and the bench to completion (report its output tail).`, { label: 'WP8:alloc-bench', phase: 'Proof', schema: OUT }),

  () => A(SHARED + `
## Your package: WP9 — docs (DESIGN §12 WP9, §9 docs row, §2, §2.4, §4.8 time-model paragraph, §5, §6, §7)
Write ${W}/build/docs/API.md: retention rule + per-instance footprint FIRST; the §4.8 "Time model, in plain language" paragraph verbatim; every §2.1/§2.2/§2.3 member with signature, semantics, errors, alias; Recur's third argument and h:SetCatchUp; the three verbs Stop/Complete/Destroy side by side incl. the Fired/Restart/Clear notes; the §5 state matrix; the "not per-frame" list; Now() vs GetBase(); the §2.4 touch patterns and the "Reuse over recreate" paragraph with the exact numbers (576 B, ~250 ns, ~100–300 ns, 0 B, ~5.8 MB/s); the §6 wiring contract (the three legacy TickAPI blocks → Veron.new + connect, Stepped's dt is the 2nd argument, the DV-23 one-line guard recommendation, optional MaxDt / MaxCatchUp = 1 for a render instance, SafeStopClock and AfterNotTouchedClass keep working and why).
Write ${W}/build/docs/MIGRATION.md: §7.1 preserved shapes, §7.2 M0–M3 with verify/rollback, §7.3 every DV row (DV-1…DV-24 as listed, same ids), §7.4 prior 2.0.0 → 3.0.0 rename table, the ≤ 26 recur-nested files with the "one parent-period EARLIER" warning (take the file list from ${W}/research/callsites.md — the recur+delay-on-one-scheduler survey — and cite it), the out-of-scope list (RoBase plugin, SyncedTimer, DashStacks:22, UpdateAction_ActionServerHitScan:113, CharacterSheet:772).
Write ${W}/build/tests/spec/docs/docs_spec.luau with the nine §9 docs tests (read the md files with @lune/fs relative to process.cwd() = W; assert presence of every §2 member name, the rule sentences, every DV id, M0–M3, the prior names, the reuse numbers). Run it green.`, { label: 'WP9:docs', phase: 'Proof', schema: OUT }),
])).filter(Boolean)
log(`Stage D done: ${stageD.length}/3; blockers: ${stageD.flatMap(r => r.blockers).length}`)

// ------------------------------------------------------------------ Stage E
phase('Final')
const FINAL = SHARED + `
## Your task: FINAL INTEGRATOR — whole suite green, bench run, build report
Run \`lune run build/tests/run.luau\`. Fix every failure with the integrator rules (code first; a spec only when it provably contradicts DESIGN.md, cite the section; append decisions to BUILD-NOTES.md under "Final integration"). Address the Stage D blockers: ${JSON.stringify(stageD.map(r => ({ files: r.files, blockers: r.blockers, deviations: r.deviations })))}. Up to 6 fix cycles. Then run \`lune run build/bench/bench.luau\` and confirm it completes with its invariant assertions passing. Then write ${W}/build/BUILD-REPORT.md: suite line (counts), spec-file/test inventory vs the §9 plan (name every §9 test that is MISSING or renamed), bench headline numbers (P2 idle, P7 recurring, P8 mixed, P14 legacy ratio, P15 per-instance), the deviations list consolidated from BUILD-NOTES.md, and an honest "known gaps" section. Report the final suite line verbatim.`
const fin = await A(FINAL, { label: 'final:integrate', phase: 'Final', schema: OUT })
const finalGreen = fin && /\b0 failed\b/.test(fin.testsRun)
log(`FINAL: ${fin ? fin.testsRun.slice(-80) : 'FAILED'}`)
return { stageA, stageB, integ, green, stageD, fin, finalGreen }
